DECLARE
  AVERAGEVAL NUMBER;
BEGIN

  AVERAGEPRICE(
    AVERAGEVAL => AVERAGEVAL
  );
  /* Legacy output: 
DBMS_OUTPUT.PUT_LINE('AVERAGEVAL = ' || AVERAGEVAL);
*/ 
  :AVERAGEVAL := AVERAGEVAL;
--rollback; 
END;
